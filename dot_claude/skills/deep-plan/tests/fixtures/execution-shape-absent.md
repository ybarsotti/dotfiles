# Absent Execution Shape Fixture

No `## Execution shape` section anywhere in this file — a legitimate,
serial-only plan. Must degrade quietly (mode: null, exit 0), not fail.

## Affected files

- `justfile` — test
